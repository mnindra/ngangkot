import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  content: {
    backgroundColor: '#fff'
  },
  map: {
    width: '100%',
    height: 300
  }
});

export default styles;